apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: kubeflow
spec:
  rules:
    - host: kf.{{ cluster_dns }}
      http:
        paths:
          - path: /
            backend:
              serviceName: istio-ingressgateway
              servicePort: 80
